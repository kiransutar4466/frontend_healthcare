import React from 'react'
import { useSelector } from 'react-redux'
// import { componentKey } from '../Authentication/LoginSlice'

const Cart = () => {
    // const { loadingState } = useSelector(state => state[componentKey])

    return (
        <div>
            Cart
        </div>
    )
}

export default Cart
